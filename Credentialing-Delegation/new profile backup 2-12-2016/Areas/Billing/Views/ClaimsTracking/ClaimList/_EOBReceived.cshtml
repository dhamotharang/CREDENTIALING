@model List<PortalTemplate.Areas.Billing.Models.Claims_Tracking.EobReceivedClaimViewModel>
<div class="table-responsive">
    <table class="table table-striped table-hover table-responsive custom-thead-back custom-thead-font custom-tbody custom-table-striped">
        <thead class="theme_thead">
            <tr>
                <th>Sl.No </th>
                <th>Claim ID </th>
                <th>Payer</th>
                <th>Member</th>
                <th>Rendering Provider </th>
                <th>DOS From</th>
                <th>DOS To</th>
                <th>Claimed Amount</th>
                <th>Sent On</th>
                <th>Paid Amount</th>
                <th>Paid On</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@( ((Int32)1) + @Model.IndexOf(item))</td>
                    <td class="ClaimIdTd">@item.ClaimId</td>
                    <td>@item.PrimaryPayer</td>
                    <td>@item.Member</td>
                    <td>@item.RenderingProvider</td>
                    <td>@item.DosFrom.Value.ToShortDateString()</td>
                    <td>@item.DosTo.Value.ToShortDateString()</td>
                    <td>@item.ClaimedAmount</td>
                    <td>@item.SentOn.Value.ToShortDateString()</td>
                    <td>@item.PaidAmount</td>
                    <td>@item.PaidOn.Value.ToShortDateString()</td>
                    <td>
                        <button class="btn btn-info btn-xs margin_left_12px editBtn" data-toggle="tooltip" title="View Claim"><i class="fa fa-edit"> </i></button>
                        <button class="btn btn-info btn-xs margin_left_12px eobBtn" data-toggle="tooltip" title="View EOB"><i class="fa fa-file-o"> </i></button>

                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>



