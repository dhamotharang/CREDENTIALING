@model AHC.CD.WebUI.MVC.Areas.Profile.Models.WorkHistory.WorkGapViewModel

        <div class="col-md-12">
            <div class="form-group col-md-3">
                @Html.LabelFor(p => p.StartDate, htmlAttributes: new { @class = "control-label small" })
                <div>
                    <i class="data-label">{{wg.StartDate}}</i>
                </div>
            </div>

            <div class="form-group col-md-3">
                @Html.LabelFor(p => p.EndDate, htmlAttributes: new { @class = "control-label small" })
                <div>
                    <i class="data-label">{{wg.EndDate}}</i>
                </div>
            </div>

            <div class="form-group col-md-3">
                @Html.LabelFor(p => p.Description, htmlAttributes: new { @class = "control-label small" })
                <div>
                    <i class="data-label">{{wg.Description}}</i>
                </div>
            </div>

        </div>
