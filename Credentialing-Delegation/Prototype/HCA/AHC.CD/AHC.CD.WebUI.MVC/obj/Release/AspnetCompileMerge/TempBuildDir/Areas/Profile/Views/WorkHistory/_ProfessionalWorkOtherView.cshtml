@model AHC.CD.WebUI.MVC.Areas.Profile.Models.WorkHistory.ProfessionalWorkExperienceViewModel
<fieldset>
    <legend>
        Other
    </legend>
    <div class="panel-body">

        <div class="col-md-12">
            <div class="form-group col-md-3">
               @* @Html.LabelFor(p => p.DepartureReason, htmlAttributes: new { @class = "control-label small" })*@
                <label class="control-label small"> Departure Reason</label>
                <div>
                    <i class="data-label">{{workHistory.DepartureReason}}</i>
                </div>
            </div>
           
            <div class="form-group col-md-3">
                @*@Html.LabelFor(p => p.StartDate, htmlAttributes: new { @class = "control-label small" })*@
                <label class="control-label small">Start Date</label>
                <div>
                    <i class="data-label">{{workHistory.StartDate}}</i>
                </div>
            </div>

            <div class="form-group col-md-3">
                @*@Html.LabelFor(p => p.EndDate, htmlAttributes: new { @class = "control-label small" })*@
                <label class="control-label small">End Date</label>
                <div>
                    <i class="data-label">{{workHistory.EndDate}}</i>
                </div>
            </div>

        </div>
    </div>
</fieldset>
