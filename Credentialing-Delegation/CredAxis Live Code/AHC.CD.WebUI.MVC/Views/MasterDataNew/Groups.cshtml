@using Microsoft.AspNet.Identity;
@using Microsoft.AspNet.Identity.Owin;


@{
    ViewBag.Title = "Groups";
}
@section AddToHead{
<link href="~/Content/SharedCss/app.css" rel="stylesheet" />
<script src="~/Scripts/Lib/Jquery/jquery-1.11.0.js"></script>
<script src="~/Scripts/Lib/Angular/angular.min.js"></script>
<script src="~/Scripts/Lib/AHC_Lib/UtilDirectivesLib.js"></script>
<script src="~/Scripts/Lib/Angular-DatePicker/ui-bootstrap-tpls-0.12.0.min.js"></script>
<script src="~/Scripts/Custom/MasterDataNew/GroupsController.js"></script>
}
<div id="page-wrapper">
    <div class="container-fluid wrap-words">
        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <ol class="breadcrumb">
                    <li>
                        <a href="@System.Configuration.ConfigurationManager.AppSettings["AppRoot"]/MasterDataNew"><i class="fa fa-database"></i> Master Data</a>
                    </li>
                    <li>
                        <i class="fa fa-group"></i> IPA / Group
                    </li>
                </ol>
            </div>
        </div>
        <!-- /.row -->
        <!-- Add Provider content form div -->
        <div class="row" ng-app="masterDataGroup" ng-controller="masterDataGroupController" ng-cloak>
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <i class="fa fa-group"></i> IPA / Group Management 
                        <span class="pull-right">
                            @if (HttpContext.Current.User.Identity.IsAuthenticated)
                            {
                                var roles = Context.GetOwinContext().GetUserManager<AHC.CD.WebUI.MVC.ApplicationUserManager>().GetRoles(User.Identity.GetUserId());
                                if (roles.Contains("CRA"))
                                {
                                    <button ng-disabled="disableAdd" ng-click="addGroup()" class="btn btn-xs btn-primary"><i class="fa fa-plus"></i> Add</button>
                                }
                            }
                        </span>
                    </div>
                    <div class="panel-body">
                        <div ng-show="activeMessageDiv=='Group'" class="alert alert-success" role="alert">
                            @*<button ng-click="closeAlertMessage('')" class="close"><span>&times;</span></button>*@
                           {{messageDesc}}
                        </div>
                        <div ng-show="activeMessageDiv=='GroupError'" class="alert alert-danger" role="alert">
                            @*<button ng-click="closeAlertMessage('')" class="close"><span>&times;</span></button>*@
                           {{messageDesc}}
                        </div>
                        <table class="table table-striped table-bordered table-condensed table-hover">
                            <thead>
                                <tr>
                                    <th>IPA / Group Name *</th>
                                    @*<th>Code</th>*@
                                    <th>Description</th>
                                    <th>TaxId</th>
                                    <th>NPI Number</th>
                                    <th>Status</th>
                                    @*<th>Date Modified</th>*@
                                    <th>Action</th>
                                </tr>
                                <tr ng-show="Groups.length>0">
                                    <td><input type="text" placeholder="Filter IPA/Group Name" class="form-control input-sm" ng-model="search.Name" ng-change="filterData()" /></td>
                                    <td><input type="text" placeholder="Filter Description" class="form-control input-sm" ng-model="search.Description" ng-change="filterData()" /></td>
                                    <td><input type="text" placeholder="Filter Tax Id" class="form-control input-sm" ng-model="search.TaxId" ng-change="filterData()" /></td>
                                    <td><input type="text" placeholder="Filter NPI Number" class="form-control input-sm" ng-model="search.NPINumber" ng-change="filterData()" /></td>
                                    <td><input type="text" placeholder="Filter Status" class="form-control input-sm" ng-model="search.Status" ng-change="filterData()" /></td>
                                    <td></td>
                                </tr>
                            </thead>
                            <tbody ng-show="Groups.length>0">
                                <tr ng-repeat="group in GroupsData=(Groups | filter:{Name:search.Name,Description:search.Description,Status:search.Status}) track by $index" ng-include="getTemplate(group)" ng-show="IsValidIndex($index)"></tr>
                            </tbody>
                            <tbody ng-show="GroupsData.length==0 || Groups.length==0 ">
                                <tr class="text-center">
                                    <td colspan="7"><b>No Data Available</b></td>
                                </tr>
                            </tbody>
                        </table>
                        @*| filter{Name:search.Name,Description:search.Description,Status:search.Status}*@
                        <script type="text/ng-template" id="displayGroup">
                            <td>{{group.Name}}</td>
                            @*<td>{{group.Code}}</td>*@
                            <td>{{group.Description || 'Not Available'}}</td>
                            <td>{{group.TaxId || 'Not Available'}}</td>
                            <td>{{group.NPINumber || 'Not Available'}}</td>
                            <td>{{group.Status}}</td>
                            @*<td>{{group.LastModifiedDate | date : 'MM-dd-yyyy'}}</td>*@
                            <td>
                                @if (HttpContext.Current.User.Identity.IsAuthenticated)
                                {
                                    var roles = Context.GetOwinContext().GetUserManager<AHC.CD.WebUI.MVC.ApplicationUserManager>().GetRoles(User.Identity.GetUserId());
                                    if (roles.Contains("CRA"))
                                    {
                                        <button ng-disabled="disableEdit" ng-click="editGroup(group)" class="btn btn-xs btn-primary" tooltip data-toggle="tooltip" data-placement="right" title="Edit"><i class="fa fa-edit"></i></button>
                                    }
                                }
                                
                            </td>
                        </script>
                        <script type="text/ng-template" id="editGroup">
                            <td><input type="text" ng-model="tempGroup.Name" class="form-group form-control input-sm inputMarginBottoomZero" />
                                <span class="text-danger" ng-if="error && !tempGroup.Name">{{error}}</span>
                                <span class="text-danger" ng-if="existErr && tempGroup.Name">{{existErr}}</span></td>
                            @*<td><input type="text" ng-model="tempGroup.Code" class="form-group form-control input-sm inputMarginBottoomZero" /></td>*@
                            <td><input type="text" ng-model="tempGroup.Description" class="form-group form-control input-sm inputMarginBottoomZero" /></td>
                            <td><input type="text" ng-model="tempGroup.TaxId" class="form-group form-control input-sm inputMarginBottoomZero" /></td>
                            <td><input type="text" ng-model="tempGroup.NPINumber" class="form-group form-control input-sm inputMarginBottoomZero" /></td>
                            <td>{{tempGroup.Status}}</td>
                            @*<td>{{tempGroup.LastModifiedDate | date : 'MM-dd-yyyy'}}</td>*@
                            <td>
                                <button ng-if="!tempGroup.GroupID" ng-click="saveGroup($index)" class="btn btn-xs btn-primary">Save</button>
                                <button ng-if="tempGroup.GroupID" ng-click="updateGroup($index)" class="btn btn-xs btn-primary">Update</button>
                                <button ng-if="tempGroup.GroupID" ng-click="reset()" class="btn btn-xs btn-warning">Cancel</button>
                                <button ng-if="!tempGroup.GroupID" ng-click="cancelAdd()" class="btn btn-xs btn-warning">Cancel</button>
                            </td>
                        </script>
                        <div class="clearfix"></div>
                        <div ng-if="CurrentPage.length > 0" ccolicensedetailstable></div>
                        <pagination ng-show="CurrentPage.length > 0" total-items="bigTotalItems" ng-model="bigCurrentPage" max-size="maxSize" class="pagination-sm" ng-change="pageChanged(bigCurrentPage)" boundary-links="true" rotate="false" num-pages="numPages" style="margin:0;"></pagination>
                       
                    </div>
                </div>
            </div>
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</div>