<div id="SupervisingProvider">
    <fieldset>
        <legend>
            Supervising Provider
        </legend>
        <table ng-hide="PracticeLocationDetailHistory.SupervisingProviders.length > 0 || visibilityThirdControl == 'SupervisingProviderEdit'" class="table table-bordered table-condensed customtbodyStyle table-striped">
            <thead>
                <tr>
                    <td>
                        No History Available for Supervising Providers for thi Practice Location!
                    </td>
                </tr>
            </thead>
        </table>
        <table ng-if="PracticeLocationDetailHistory.SupervisingProviders.length>0" class="table table-bordered table-condensed customtbodyStyle table-striped">
            <thead>
                <tr>
                    <th>First Name</th>
                    <th>Middle name</th>
                    <th>Last Name</th>
                    <th>NPI Number</th>
                </tr>
            </thead>
            <tbody ng-repeat="provider in PracticeLocationDetailHistory.SupervisingProviders">
                <tr>
                    <td>{{provider.FirstName || 'Not Available'}}</td>
                    <td>{{provider.MiddleName || 'Not Available'}}</td>
                    <td>{{provider.LastName || 'Not Available'}}</td>
                    <td>{{provider.NPINumber || 'Not Available'}}</td>
                </tr>
            </tbody>
        </table>
    </fieldset>
</div>